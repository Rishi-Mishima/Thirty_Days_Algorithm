## 102 二叉树的层序遍历

### DFS递归

```java
void dfs(TreeNode root) {
    if (root == null) {
        return;
    }
    dfs(root.left);
    dfs(root.right);
}
```



### BFS基础遍历

```java
void bfs(TreeNode root) {
    Queue<TreeNode> queue = new ArrayDeque<>();
    queue.add(root);
    while (!queue.isEmpty()) {
        TreeNode node = queue.poll(); // Java 的 pop 写作 poll()
        if (node.left != null) {
            queue.add(node.left);
        }
        if (node.right != null) {
            queue.add(node.right);
        }
    }
}
```

BFS 遍历使用队列数据结构



#### 使用Deque 

[廖雪峰: 使用Deque](https://liaoxuefeng.com/books/java/collection/deque/index.html)

Duque : 双端队列

Java集合提供了接口`Deque`来实现一个双端队列，它的功能是：

- 既可以添加到队尾，也可以添加到队首；
- 既可以从队首获取，又可以从队尾获取



### BFS 使用1: 层序遍历 (Level-Order Traversal)

![二叉树的层序遍历](https://pic.leetcode.cn/ce41cf1cabfa7a56387f63d927c8819fe1479ecf6f193a2a1b47964f5a8d1c8e.jpg)

[图片](https://pic.leetcode.cn/ce41cf1cabfa7a56387f63d927c8819fe1479ecf6f193a2a1b47964f5a8d1c8e.jpg)



BFS 遍历: 一维数组

层序遍历: 二维数组

- 我们 **无法区分队列中的结点来自哪一层**
- 因此，我们需要稍微修改一下代码，在每一层遍历开始前，先记录队列中的结点数量 *n*（也就是这一层的结点数量），然后一口气处理完这一层的 *n* 个结点

```java
// 二叉树的层序遍历
void bfs(TreeNode root) {
    Queue<TreeNode> queue = new ArrayDeque<>();
    queue.add(root);
    while (!queue.isEmpty()) {
        int n = queue.size();
        for (int i = 0; i < n; i++) { 
            // 变量 i 无实际意义，只是为了循环 n 次
            TreeNode node = queue.poll();
            if (node.left != null) {
                queue.add(node.left);
            }
            if (node.right != null) {
                queue.add(node.right);
            }
        }
    }
}
```

可以看到，在 while 循环的每一轮中，都是将当前层的所有结点出队列，再将下一层的所有结点入队列，这样就实现了层序遍历。

```java
public List<List<Integer>> levelOrder(TreeNode root) {
    List<List<Integer>> res = new ArrayList<>(); // 1. 用于保存最终按层分组的结果
  // List<List<Integer>> - 因为二叉树的层序遍历需要返回的是 “按层分组” 的结果，而不是一个扁平的一维列表。

    Queue<TreeNode> queue = new ArrayDeque<>(); // 2. 创建辅助队列
    if (root != null) {
        queue.add(root);                        // 3. 特判：根节点不为空时入队，开启 BFS
    }

    while (!queue.isEmpty()) {                  // 4. 当队列不为空，说明还有待处理的节点
        int n = queue.size();                   // 5. 关键点：获取【当前层】的节点个数
        List<Integer> level = new ArrayList<>();// 6. 用于存放【当前层】所有节点的值

        for (int i = 0; i < n; i++) {           // 7. 精确循环 n 次，只处理当前层的节点
            TreeNode node = queue.poll();       // 8. 节点出队
            level.add(node.val);                // 9. 将节点值存入当前层列表

            // 10. 将下一层的子节点依次入队（按从左到右的顺序）
            if (node.left != null) {
                queue.add(node.left);
            }
            if (node.right != null) {
                queue.add(node.right);
            }
        }
        res.add(level);                         // 11. 将收集好的当前层结果加入最终列表
    }

    return res;                                 // 12. 返回遍历结果
}
```

二维数组: 

```java
List<List<Integer>> res = new ArrayList<>(); // 存放“所有层”的总大组

while (!queue.isEmpty()) {
    List<Integer> level = new ArrayList<>(); // 专门存放“当前这一层”的小组
    
    for (int i = 0; i < n; i++) {
        level.add(node.val); // 1. 把当前节点值塞进“当前层”的小组里
    }
    
    res.add(level);          // 2. 把收集好的一整层（小组），作为一个整体塞进总大组里
}
```



核心思想: 

> - **核心数据结构**：使用 **队列（`Queue`）**，遵循 **先进先出（FIFO）** 的原则。
> - **分层处理机制**：在进入每一层的循环时，通过 `int n = queue.size()` **提前记录当前层的节点总数**。这一步是把“按层分组”和“普通 BFS”区分开的关键，保证了内层 `for` 循环只处理属于当前层的节点。