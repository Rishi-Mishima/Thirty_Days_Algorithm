|                           LeetCode                           |                             力扣                             | 难度 |
| :----------------------------------------------------------: | :----------------------------------------------------------: | :--: |
|  [55. Jump Game](https://leetcode.com/problems/jump-game/)   |   [55. 跳跃游戏](https://leetcode.cn/problems/jump-game/)    |      |
| [45. Jump Game II](https://leetcode.com/problems/jump-game-ii/) | [45. 跳跃游戏 II](https://leetcode.cn/problems/jump-game-ii/) |      |

回溯算法的剪枝优化是提前排除不可能的答案，使树结构尽可能小，最终的算法复杂度一般是指数级别；动态规划的备忘录优化是为了避免重复计算，把树形结构优化成线性结构，最终的算法复杂度一般是多项式级别。

上述两种优化只是减少了无效穷举，但依然都穷举了所有可行解，从而试图寻找最优的那个解。

贪心算法和它们的区别是：有些问题其实不需要完整地穷举所有可行解，就可以推导出最优解。这样一来，进一步减少了穷举空间，效率自然会更高。



### 选零钱 

> 比方说现在有两种钞票，面额分为为 1 元和 100 元，每种钞票的数量无限，但现在你只能选择 10 张，请问你应该如何选择，才能使得总金额最大？

```java
// 定义：做 n 次选择，返回可以获得的最大金额
int findMax(int n) {
    if (n == 0) return 0;
    
    // 这次选择 1 元，然后递归求解剩下的 n - 1 次选择的最大值
    int result1 = 1 + findMax(n - 1);
    // 这次选择 100 元，然后递归求解剩下的 n - 1 次选择的最大值
    int result2 = 100 + findMax(n - 1);

    // 返回两种选择中的最大值
    return Math.max(result1, result2);
}// 定义：做 n 次选择，返回可以获得的最大金额
int findMax(int n) {
    if (n == 0) return 0;
    
    // 这次选择 1 元，然后递归求解剩下的 n - 1 次选择的最大值
    int result1 = 1 + findMax(n - 1);
    // 这次选择 100 元，然后递归求解剩下的 n - 1 次选择的最大值
    int result2 = 100 + findMax(n - 1);

    // 返回两种选择中的最大值
    return Math.max(result1, result2);
}
```

1. Base Case（基准情况）

```java
if (n == 0) return 0;
```

如果不需要做任何选择了（`n == 0`），那么获得的金额自然是 0。这是递归的出口。

2. 做出选择并递归

```java
// 这次选择 1 元，然后递归求解剩下的 n - 1 次选择的最大值
int result1 = 1 + findMax(n - 1);
// 这次选择 100 元，然后递归求解剩下的 n - 1 次选择的最大值
int result2 = 100 + findMax(n - 1);
```

这里模拟了当前这一步的两种可能性：

- **路径 A**：假设我这次拿了 1 元。那么剩下的问题就变成了“在剩下的 `n-1` 次选择中，最多能拿多少钱？”即 `findMax(n - 1)`。总金额就是 `1 + 剩下的最大值`。
- **路径 B**：假设我这次拿了 100 元。同理，剩下的问题是 `findMax(n - 1)`。总金额就是 `100 + 剩下的最大值`。

注意：虽然代码里写了两次 `findMax(n - 1)`，但在计算机执行时，它会分别沿着这两条路往下走，直到触底反弹。

3. 取最优解

```java
return Math.max(result1, result2);
```

既然我有两条路可以走，我就要看看哪条路最终得到的钱更多，返回那个更大的值。

##### 这是“暴力穷举”？

想象一下，如果你要做 3 次选择，这棵递归树长这样：

```
               第1次选择
              /        \
           选1元       选100元
          /    \       /      \
       选1元  选100元 选1元   选100元
       ...     ...    ...      ...
```

每次选择都有 2 种可能，做 `n` 次选择就有 2^n 种组合。这段代码会把这 2n2*n* 种组合全部算出来，然后比较出最大的那个。



##### 为什么可以优化成贪心？

正如文章所说，你仔细观察会发现： `result1` 和 `result2` 中都包含 `findMax(n - 1)`。 因为 `100 > 1`，所以 `100 + findMax(n - 1)` 永远大于 `1 + findMax(n - 1)`。

既然不管后面怎么选，当前选 100 元总是比选 1 元好，那我们根本没必要去计算 `result1`，也没必要递归。直接每次都选 100 元就行了：

```java
// 贪心优化后
int findMax(int n) {
    return 100 * n;
}
```



##### 优化

```java
// 优化一、没必要对两种选择进行比较了
int findMax(int n) {
    if (n == 0) return 0;
    int result = 100 + findMax(n - 1);
    return result;
}

// 优化二、递归改为迭代
int findMax(int n) {
    int result = 0;
    for (int i = 0; i < n; i++) {
        result += 100;
    }
    return result;
}

// 优化三、直接计算结果就行了
int findMax(int n) {
    return 100 * n;
}
```

这就是贪心算法，复杂度从 O(2n) 优化到了 O(1)，堪称离谱。



### 贪心选择性质

现在有两种钞票，面额分别为 1 元和 100 元，每种钞票的数量无限。现在给你一个目标金额 `amount`，请问你最少需要多少张钞票才能凑出这个金额？

这道题其实就是 [动态规划解题套路框架](https://labuladong.online/zh/algo/essential-technique/dynamic-programming-framework/) 中讲解的凑零钱问题。

**为了方便讲解，本文开头的最大金额问题我们称为「问题一」，这里的最少钞票数量问题我们称为「问题二」**。

我们是如何解决问题二的？首先也是抽象出递归树，写出指数级别的暴力穷举算法，然后发现了重叠子问题，于是用备忘录消除重叠子问题，这就是标准的动态规划算法的求解过程，不能再优化了。

所以，问题二和问题一到底有什么区别？区别在于，前者没有贪心选择性质，而后者有。



>  贪心选择性质
>
> > 贪心选择性质就是说能够通过局部最优解**直接**推导出全局最优解。



结合案例来说，对于问题一，局部最优解就是每次都选择 100 元，因为 100 > 1；对于问题二，局部最优解也是每次都选择 100 元，因为每张面额尽可能大，所需的钞票数量就能尽可能少。

但区别在于，问题一中每一次选择的局部最优解组合起来就是全局最优解，而问题二中不是。

比方说目标金额 `amount = 3`，虽然每次选择 100 元是局部最优解，但想凑出 3 元，只能选择 3 张 1 元，局部最优解不一定能构成全局最优解。

对于问题二的场景，不符合贪心选择性质



```markdown
贪心选择性质 vs 最优子结构

动态规划解题套路框架
 中提到，算法问题必须要有「最优子结构」性质，才能通过子问题的最优解推导出全局最优解，这是动态规划算法的基础。

那么就有读者搞不清楚了，这个「贪心选择性质」和「最优子结构」有什么区别？

> 最优子结构的意思是说，现在我已经把所有子问题的最优解都求出来了，然后我可以基于这些子问题的最优解推导出原问题的最优解。

**贪心选择**性质的意思是说，**我只需要进行一些局部最优的选择策略**，就能直接知道哪个子问题的解是最优的了，且这个局部最优解可以推导出原问题的最优解。此时此刻我就能知道，不需要等到所有子问题的解算出来才知道。

所以贪心算法的效率一般都比较高，因为它不需要遍历完整的解空间。
```



### 力扣题实战

#### 力扣55. 跳跃游戏

第一道例题，力扣第 55 题「[跳跃游戏](https://leetcode.cn/problems/jump-game/)」：



```java
class Solution {
    public boolean canJump(int[] nums) {
        int n = nums.length;
        int farthest = 0;
        for (int i = 0; i < n - 1; i++) {
            // 不断计算能跳到的最远距离
            farthest = Math.max(farthest, i + nums[i]);
            // 可能碰到了 0，卡住跳不动了
            if (farthest <= i) {
                return false;
            }
        }
        return farthest >= n - 1;
    }
}
```



#### 力扣45. 跳跃游戏II

接下来看第二道例题，力扣第 45 题「[跳跃游戏 II](https://leetcode.cn/problems/jump-game-ii/)」：