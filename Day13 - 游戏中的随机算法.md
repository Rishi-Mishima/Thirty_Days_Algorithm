# 随机算法

|                           LeetCode                           |                             力扣                             | 难度 |
| :----------------------------------------------------------: | :----------------------------------------------------------: | :--: |
| [384. Shuffle an Array](https://leetcode.com/problems/shuffle-an-array/) | [384. 打乱数组](https://leetcode.cn/problems/shuffle-an-array/) |      |
| [382. Linked List Random Node](https://leetcode.com/problems/linked-list-random-node/) | [382. 链表随机节点](https://leetcode.cn/problems/linked-list-random-node/) |      |
| [398. Random Pick Index](https://leetcode.com/problems/random-pick-index/) | [398. 随机数索引](https://leetcode.cn/problems/random-pick-index/) |      |

前置:

- 数组基础



扫雷实现一个矩阵

```java
class Game {
    int m, n;
    // 大小为 m * n 的二维棋盘
    // 值为 true 的地方代表有雷，false 代表没有雷
    boolean[][] board;
}
```

在开始游戏时，需要在棋盘中随机生成 `k` 个雷，也就是说需要在 `board` 中生成 `k` 个不同的 `(x, y)` 坐标，且这里面 `x, y` 都是随机生成的。

对于这个需求，**首先一个优化就是对二维矩阵进行「降维打击」，把二维数组转化成一维数组**：

```java
class Game {
    int m, n;
    // 长度为 m * n 的一维棋盘
    // 值为 true 的地方代表有雷，false 代表没有雷
    boolean[] board;

  	// encode: 二维转一维
    // 将二维数组中的坐标 (x, y) 转化为一维数组中的索引
    int encode(int x, int y) {
        return x * n + y;
    }
  // 相当于跳过前面的, 然后得到想要的
  

  
    // 将一维数组中的索引转化为二维数组中的坐标 (x, y)
    int[] decode(int index) {
        return new int[] {index / n, index % n};
    }
}
```

> **二维坐标映射到一维索引，以及反向还原**。这在处理矩阵（如扫雷棋盘）时非常常用，可以把二维问题简化为一维数组操作



Encode: 解释

Let's say you have \(3\) rows of \(4\) desks. So, \(n = 4\).
You want to sit in Row \(2\), Desk \(1\).

1. **Multiply the row by the total columns:** \(2 \times 4 = 8\)
2. **Add the desk number:** \(8 + 1 = 9\)

Your seat is in box \(9\) on the computer's straight line.



Decode: 二维转一维

- **原理**：已知一维索引，反推它在原矩阵的第几行、第几列。
- `index / n`：**整数除法**。表示这个索引之前完整包含了多少行，即行号 `x`。
- `index % n`：**取余运算**。表示去掉完整行后，在当前行剩余的位置数，即列号 `y`。

假设 `n = 5`（每行 5 个元素），坐标 `(2, 3)`：

- **Encode**: `2 * 5 + 3 = 13`。说明它是第 13 个元素（从 0 开始计数）。
- **Decode**: 索引 `13`。
  - 行号：`13 / 5 = 2`
  - 列号：`13 % 5 = 3`
  - 还原为 `(2, 3)`。

这种技巧在 [洗牌算法](https://labuladong.online/zh/algo/frequency-interview/random-algorithm/#洗牌算法) 和 [水塘抽样算法](https://labuladong.online/zh/algo/frequency-interview/random-algorithm/#水塘抽样算法) 中都非常有用，因为它允许我们直接在一维空间上进行随机数生成和交换，而不必同时维护 `x` 和 `y` 两个变量。



> 但是这种算法容易出现重复的随机数

如果 `k` 比较小 `m * n` 比较大，那出现重复随机数的概率还比较低，但如果 `k` 和 `m * n` 的大小接近，那么出现重复随机数的概率非常高，算法的效率就会大幅下降。



### 洗牌算法

**第一个解决方案，我们可以换个思路，避开「在数组中随机选择 `k` 个元素」这个问题，把问题转化成「如何随机打乱一个数组」**。

现在想随机初始化 `k` 颗雷的位置，你可以先把这 `k` 颗雷放在 `board` 开头，然后把 `board` 数组随机打乱，这样雷不就随机分布到 `board` 数组的各个地方了吗？

洗牌算法，或者叫随机乱置算法就是专门解决这个问题的，我们可以看下力扣第 384 题「[打乱数组](https://leetcode.cn/problems/shuffle-an-array/)」：

```java
class Solution {
    private int[] nums;
    private Random rand = new Random();
    
    public Solution(int[] nums) {
        this.nums = nums;
    }
    
    public int[] reset() {
        return nums;
    }
    
    // 洗牌算法
    public int[] shuffle() {
        int n = nums.length;
        int[] copy =  Arrays.copyOf(nums, n);
        for (int i = 0 ; i < n; i++) {
          
          // rand.nextInt(n - i) 会生成一个 0 到 (n-i-1) 的数字
    			// 加上 i 之后，范围就变成了 i 到 n-1
            // 生成一个 [i, n-1] 区间内的随机数
            int r = i + rand.nextInt(n - i);
            // 交换 nums[i] 和 nums[r]
            swap(copy, i, r);
        }
        return copy;
    }
    
    private void swap(int[] nums, int i, int j) {
        int temp = nums[i];
        nums[i] = nums[j];
        nums[j] = temp;
    }
}
```



洗牌算法（也就是代码里用的 **Fisher-Yates 洗牌算法**，或者叫 Knuth Shuffle）其实非常精妙。

它的核心思想和我们平时打牌完全一样：**从一堆未洗过的牌里，随机抽出一张，放到洗好的牌堆里。**

**核心关键点：** 每轮挑随机数的时候，范围都在**不断缩小**。已经洗好的位置，绝对不会被后面的随机数再次抽到。这样能保证**每个排列出现的概率是完全相等的（绝对公平）**。



##### 为什么要在类成员位置写 `new Random()`？

```java
private Random rand = new Random();
```

- **复用性**：`Random` 对象创建是有开销的。如果在 `shuffle` 方法内部每次调用都 `new Random()`，不仅浪费资源，而且在极短时间内多次创建可能导致随机数种子相同，产生“伪随机”重复序列。
- **状态保持**：作为类的成员变量，它在整个 `Solution` 对象的生命周期内只初始化一次，后续每次调用 `shuffle()` 都复用同一个随机数生成器，保证效率和质量。

这行代码包含两个部分：**声明成员变量** 和 **实例化对象**。

- **`private`（私有访问修饰符）：** 意思是这个 `rand` 变量**只能在这个类（`Solution` 类）的内部使用**。别的地方（比如其他类）没办法直接通过 `solution.rand` 来调用它。这在编程里叫**封装**，目的是保护数据安全，不让外部瞎改。
- **`Random`：** 这是 Java 官方自带的一个类，专门用来**生成随机数**。
- **`rand`：** 这是你给这个随机数生成器起的名字（变量名）。
- **`= new Random();`：** 这就是“实例化”。光写 `Random rand;` 只是画了个图纸，`new Random()` 才是真正用图纸在内存里**建造了一个随机数生成器大楼**，并把它交给了 `rand` 来掌管。





##### 在哪里用了 `rand`？

它在 `shuffle` 方法的循环中被使用，用于生成随机索引：

```java
// 生成一个 [i, n-1] 区间内的随机数
int r = i + rand.nextInt(n - i);
```

- `rand.nextInt(n - i)` 会生成一个 `[0, n-i)` 之间的随机整数。
- 加上 `i` 之后，`r` 的范围就变成了 `[i, n-1]`。
- 这个 `r` 就是用来和当前位置 `i` 进行交换的目标位置，这是洗牌算法的核心步骤。



##### 为什么要 `this.nums = nums`？

这是在**构造函数**（Constructor）里最常见的一种写法，用来**区分“重名”的变量**。

```java
public class Solution {
    private int[] nums; // 1. 这是【类级别】的变量，叫成员变量

    public Solution(int[] nums) { // 2. 这是【方法级别】的变量，叫参数
        this.nums = nums; 
    }
}
```

这里有两个 `nums`：

1. 一个是写在类最上面的 `private int[] nums;`，它是属于**整个类**的资产（成员变量）。
2. 另一个是括号里的 `int[] nums`，它是外部传进来的**一包临时数据**（参数）



为什么要加 `this.`？

因为它们名字一模一样！如果不加 `this.`，直接写 `nums = nums;`，Java 编译器就会犯迷糊：“你是在把这包临时数据自己赋值给自己吗？”（这叫**变量遮蔽**）。

- **`this` 代表当前对象自己。**
- **`this.nums`** 特指“**我这个类自带的那个 `nums` 变量**”。
- **等号右边的 `nums`** 代表“**外面传进来的那包参数**”。

**大白话总结：** 这句话的意思就是：“把我刚收到的这包传进来的数组（右边的 `nums`），妥善保存在我自己家的大仓库里（左边的 `this.nums`），这样接下来的 `reset()` 和 `shuffle()` 方法才能随时去仓库里拿着用。”

> 如果名字不一样就this一般还是要加上,  -----> **统一性**：有些人喜欢代码风格统一，不管变量名有没有冲突，一律加上 `this.`，这样一眼就能看出“这是在操作类成员变量”。

```java
public Solution(int[] nums) {
    this.nums = nums;
}
```

- **保存原始数据**：题目要求实现 `reset()` 方法，将数组恢复到初始状态。
- **引用传递**：在 Java 中，数组是引用类型。构造函数传入的 `nums` 是用户提供的原始数组。通过 `this.nums = nums`，我们将这个原始数组的引用保存到成员变量中。
- **实现 Reset**：当用户调用 `reset()` 时，直接返回这个保存好的 `nums` 即可，因为我们在 `shuffle()` 中操作的是它的拷贝（`Arrays.copyOf`），并没有修改原始数组的内容。

**总结逻辑流：**

1. **构造函数**：存下原始数组 `nums`。
2. **shuffle**：复制一份数组，利用成员变量 `rand` 生成随机索引进行交换，返回打乱后的副本。
3. **reset**：直接返回构造函数里存下的原始 `nums`。



洗牌算法的时间复杂度是 O(N)，而且逻辑很简单，关键在于让你证明为什么这样做是正确的。排序算法的结果是唯一可以很容易检验的，但随机乱置算法不一样，乱可以有很多种，你怎么能证明你的算法是「真的乱」呢？

**分析洗牌算法正确性的准则：产生的结果必须有 `n!` 种可能**。这个很好解释，因为一个长度为 `n` 的数组的全排列就有 `n!` 种，也就是说打乱结果总共有 `n!` 种。算法必须能够反映这个事实，才是正确的。

有了这个原则再看代码应该就容易理解了：

对于 `nums[0]`，我们把它随机换到了索引 `[0, n)` 上，共有 `n` 种可能性；

对于 `nums[1]`，我们把它随机换到了索引 `[1, n)` 上，共有 `n - 1` 种可能性；

对于 `nums[2]`，我们把它随机换到了索引 `[2, n)` 上，共有 `n - 2` 种可能性；

以此类推，该算法可以生成 `n!` 种可能的结果，所以这个算法是正确的，能够保证随机性。



### 水塘抽样算法

学会了洗牌算法，扫雷游戏的随机初始化问题就解决了。不过别忘了，洗牌算法只是一个取巧方案，我们还是得面对「在若干元素中随机选择 `k` 个元素」这个终极问题。

> 数据流非常大（甚至无穷大），或者数据总数未知，而你的内存非常小，怎么在只遍历一遍数据的情况下，公平地随机抽取 k 个样本？



#### 终极简化版：只抽 1 个样本 (k = 1)

想象一下，你是一个面试官，门外有一条很长很长的队伍，求职者一个接一个进来面试。你不知道后面还有多少人，但你需要在所有来面试的人里**绝对公平地选出 1 个人**发 Offer。

如果面试完第 100 个人，你想让他拿 Offer 的概率是 $\frac{1}{100}$，这该怎么做？

水塘抽样的做法绝了：

- **第 1 个人**进来：没得选，只能先让他拿到 Offer。
- **第 2 个人**进来：你抛一枚硬币。有 $\frac{1}{2}$ 的概率换成第 2 个人，$\frac{1}{2}$ 的概率保留第 1 个人。
- **第 3 个人**进来：你摇一个 3 面的骰子。有 $\frac{1}{3}$ 的概率换成第 3 个人，$\frac{2}{3}$ 的概率保留原来的人。
- ……
- **第 i 个人**进来：你摇一个 i 面的骰子（或者摇一个 1 到 i 的随机数）。如果摇到了 1（概率是 1/i），你就**狠心抛弃当前拿 Offer 的人，换成这第 i个人**；否则，继续保留原配

核心就是一句话：

**新来的元素（第 $i$ 个）有没有资格进盒子？**

看它摇出来的随机数 $r$ 是多少。如果 $r$ 刚好能对上盒子的座位号（$r \le 3$），它就强行坐到那个座位上，把原来的老员工踢走；如果座位号对不上，新员工就直接淘汰。

洗牌算法用 ==数组==

- 如果用链表呢? 
  - 在一条链表中随机选择 `k` 个元素，肯定不能再用洗牌算法来蒙混过关了。
- 或者 扫雷游戏中棋盘的长和宽非常大，已经不能在内存中装下一个大小为 `m * n` 的 `board` 数组了，我们只能维护一个大小为 `k` 的数组记录雷的位置：

```java
class Game {
    // 棋盘的行数和列数（非常大）
    int m, n;
    // 长度为 k 的数组，记录 k 个雷的一维索引
    int[] mines;

    // 将二维数组中的坐标 (x, y) 转化为一维数组中的索引
    int encode(int x, int y) {
        return x * n + y;
    }

    // 将一维数组中的索引转化为二维数组中的坐标 (x, y)
    int[] decode(int index) {
        return new int[] {index / n, index % n};
    }
}
```

这样的话，我们必须想办法在 `[0, m*n)` 中随机选取 `k` 个不同的数字了。

**这就是常见的随机抽样场景，常用的解法是水塘抽样算法（Reservoir Sampling）**。水塘抽样算法是一种随机概率算法，会者不难，难者不会。

我第一次见到这个算法问题是谷歌的一道算法题：给你一个**未知长度**的单链表，请你设计一个算法，**只能遍历一次**，随机地返回链表中的一个节点。力扣第 382 题「[链表随机节点](https://leetcode.cn/problems/linked-list-random-node/)」中的进阶提问也是类似的问题。

这里说的随机是均匀随机（uniform random），也就是说，如果有 `n` 个元素，每个元素被选中的概率都是 `1/n`，不可以有统计意义上的偏差。

一般的想法就是，我先遍历一遍链表，得到链表的总长度 `n`，再生成一个 `[0,n-1)` 之间的随机数为索引，然后找到索引对应的节点。但这不符合只能遍历一次链表的要求。

这个问题的难点在于随机选择是「动态」的，比如说你现在你已经遍历了 5 个元素，你已经随机选取了其中的某个元素 `a` 作为结果，但是现在再给你一个新元素 `b`，你应该留着 `a` 还是将 `b` 作为结果呢？以什么逻辑做出的选择，才能保证你的选择方法在概率上是公平的呢？

> **先说结论，当你遇到第 `i` 个元素时，应该有 `1/i` 的概率选择该元素，`1 - 1/i` 的概率保持原有的选择**。看代码容易理解这个思路：



```java
// 返回链表中一个随机节点的值
int getRandom(ListNode head) {
    Random r = new Random();
    int i = 0, res = 0;
    ListNode p = head;
    // while 循环遍历链表
    while (p != null) {
        i++;
        // 生成一个 [0, i) 之间的整数
        // 这个整数等于 0 的概率就是 1/i
        if (0 == r.nextInt(i)) {
            res = p.val;
        }
        p = p.next;
    }
    return res;
}
```



```markdown 
选择 k 个元素的情况
- 概率一定是 k/i


```

虽然每次更新选择的概率增大了 `k` 倍，但是选到具体第 `i` 个元素的概率还是要乘 `1/k`，也就回到了上一个推导。

类似的，回到扫雷游戏的随机初始化问题，我们可以写一个这样的 `sample` 抽样函数：

```java
// 在区间 [lo, hi) 中随机抽取 k 个数字
int[] sample(int lo, int hi, int k) {
    Random r = new Random();
    int[] res = new int[k];

    // 前 k 个元素先默认选上
    for (int i = 0; i < k; i++) {
        res[i] = lo + i;
    }

    int i = k;
    // while 循环遍历数字区间
    while (i < hi - lo) {
        i++;
        // 生成一个 [0, i) 之间的整数
        int j = r.nextInt(i);
        // 这个整数小于 k 的概率就是 k/i
        if (j < k) {
            res[j] = lo + i - 1;
        }
    }
    return res;
}
```

这段代码实现的是**水塘抽样算法（Reservoir Sampling）**的一个变种，用于在区间 `[lo, hi)` 中随机抽取 `k` 个不重复的数字。

它的核心思想是：**只遍历一次数据流，就能保证每个元素被选中的概率相等。**



step 1 初始化水塘

```java
int[] res = new int[k];
// 前 k 个元素先默认选上
for (int i = 0; i < k; i++) {
    res[i] = lo + i;
}
```

- **目的**：因为我们要选 `k` 个数，所以先把区间里最前面的 `k` 个数（即 `lo` 到 `lo + k - 1`）放进结果数组 `res` 中。
- 此时，`res` 满了，但里面的数并不是最终结果，后续可能会被替换。



step 2: 遍历剩余元素并决定“去留”

```java
int i = k;
// while 循环遍历数字区间
while (i < hi - lo) {
    i++;
    // ...
}
```

- **变量 `i` 的含义**：表示当前正在处理第几个元素（从 1 开始计数）。
  - 初始 `i = k`，进入循环后先 `i++`，所以第一次循环时 `i = k + 1`，代表正在处理区间里的第 `k+1` 个元素。
  - 循环条件 `i < hi - lo` 确保遍历完区间内所有剩余的元素。



step 3: 核心逻辑

```java
    // 生成一个 [0, i) 之间的整数
    int j = r.nextInt(i);
    // 这个整数小于 k 的概率就是 k/i
    if (j < k) {
        res[j] = lo + i - 1;
    }
```

- **`r.nextInt(i)`**：生成一个 `[0, i-1]` 范围内的随机整数。
- **`if (j < k)`**：
  - 因为 `j` 有 `i` 种可能取值（0 到 i-1），其中小于 `k` 的值有 `k` 个（0 到 k-1）。
  - 所以，条件成立的概率正好是 **`k / i`**。
  - **含义**：对于当前遇到的第 `i` 个元素，我们以 `k/i` 的概率决定**是否**把它放入水塘。
- **`res[j] = lo + i - 1`**：
  - 如果决定放入，就随机替换掉水塘 `res` 中的某一个位置（索引 `j` 是随机生成的，保证了替换的随机性）。
  - `lo + i - 1` 是当前正在处理的实际数值。



为什么这样是正确的？

文章中的数学证明指出： 当处理到第 `i` 个元素时，它被选入水塘的概率是 `k/i`。而在后续的步骤中，它不被后续元素替换掉的概率累积起来，最终使得**每一个元素被留在最终结果中的概率都是 `k/N`**（N 为总元素个数）。



适用场景

这种算法特别适合**数据量极大、无法全部加载到内存**，或者**数据以流的形式出现（长度未知）**的场景。它只需要 O(1) 的额外空间（存储 k 个结果）和 O(N) 的时间复杂度。





### 蒙特卡洛验证法

力扣是如何检测上面两种算法的? 

- 检测随机算法正确性的方法：蒙特卡洛方法

往一个正方形里面随机打点，这个正方形里紧贴着一个圆，告诉你打点的总数和落在圆里的点的数量，让你计算圆周率。

<img src="https://labuladong.online/images/algo/shuffle/4.png" alt="img" style="zoom:25%;" />

这其实就是利用了蒙特卡罗方法：当打的点足够多的时候，点的数量就可以近似代表图形的面积。结合面积公式，可以很容易通过正方形和圆中点的数量比值推出圆周率的。

当然，打的点越多，算出的圆周率越准确，充分体现了大力出奇迹的道理。

```java
public static void main(String[] args) {
    // 在 [12, 22) 中随机选 3 个数
    int lo = 12, hi = 22, k = 3;
    // 记录每个元素被选中的次数
    int[] count = new int[hi - lo];
    // 重复 100 万次
    int N = 1000000;
    for (int i = 0; i < N; i++) {
        int[] res = sample(lo, hi, k);
        for (int elem : res) {
            // 对随机选取的元素进行记录
            count[elem - lo]++;
        }
    }
    System.out.println(Arrays.toString(count));
}
```



#### 拓展

洗牌算法主要用于打乱数组，比如我们在 [快速排序详解及运用](https://labuladong.online/zh/algo/practice-in-action/quick-sort/) 中就用到了洗牌算法保证快速排序的效率。

水塘抽样算法的运用更加广泛，可以在序列中随机选择若干元素，且能保证每个元素被选中的概率均等。

最后留几个拓展题目：

1、本文开头讲到了将二维数组坐标 `(x, y)` 转化成一维数组索引的技巧，那么你是否有办法把三维坐标 `(x, y, z)` 转化成一维数组的索引呢？

2、如何对带有权重的样本进行加权随机抽取？比如给你一个数组 `w`，每个元素 `w[i]` 代表权重，请你写一个算法，按照权重随机抽取索引。比如 `w = [1,99]`，算法抽到索引 0 的概率是 1%，抽到索引 1 的概率是 99%，答案见 [这篇文章](https://labuladong.online/zh/algo/frequency-interview/random-pick-with-weight/)。

3、实现一个生成器类，构造函数传入一个很长的数组，请你实现 `randomGet` 方法，每次调用随机返回数组中的一个元素，多次调用不能重复返回相同索引的元素。要求不能对该数组进行任何形式的修改，且操作的时间复杂度是 O(1)，答案见 [这篇文章](https://labuladong.online/zh/algo/data-structure/random-set/)。